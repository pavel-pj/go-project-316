version: "2"

run:
  timeout: 5m
  modules-download-mode: readonly

linters:
  enable:
    - govet
    - staticcheck
    - unused
    - ineffassign
    - bodyclose
    - noctx
    - gocritic
    - errorlint
    - nilerr
    - wrapcheck